# 지도에서 나라 찾기 (advanced-world-map-quiz)

08 문서가 요구한 **평면 세계지도 나라 찾기**. 나라 이름을 주고 아이가 지도에서
그 나라를 눌러 찾는다. 하네스 계약에 10칸이 잡혀 있었는데 제품에 화면이 없어
오래 미달로 남아 있던 항목이다 — 이번에 만들었다. 2026-09-14.

## 무엇으로 만들었나

| 조각 | 파일 | 하는 일 |
| --- | --- | --- |
| 정답 데이터 | `public/maps/world-quiz.v1.json` | 66개국 polygon. `scripts/build-world-map.mjs` 가 만든다 |
| 순수 로직 | `lib/worldMapQuiz.ts` | 투영·역변환, 점-다각형 판정, 출제, 힌트, 이름 잇기 |
| 화면 | `components/games/WorldMapQuiz.tsx` | SVG 지도, 확대·이동, 문제 패널, 결과 |
| 재현 경로 | `/ux-fixture/world-map?seed=N` | 시드 고정 — 같은 시드면 같은 문제 |
| 로비 | `components/GameRoom.tsx` 의 `worldmap` | 아이가 실제로 닿는 길 |

## 데이터 — 출처와 한계

**Natural Earth 5.1.1 (10m), public domain**, `https://www.naturalearthdata.com/about/`.
저장소에 이미 있던 `data/maps/natural-earth/` 를 그대로 쓴다. 08 §3 이 "국가 경계나
국기를 이미지 생성 모델로 그려 정답 데이터로 삼지 않는다" 고 못 박았으므로 **정답
판정에 쓰는 선은 한 획도 생성하지 않았다.** 그림으로 만든 것은 로비 아이콘 한 장뿐이고
그건 판정에 쓰이지 않는다.

원본이 13.6MB(캐나다 한 나라가 2.6MB)라 그대로는 못 보낸다 — 06 §7 의 최초 추가
전송 예산이 압축 3MB 다. Douglas-Peucker 로 줄여 **0.73MB**(점 361k → 45k, 87.5% 감소)
로 만들었고, 이 놀이를 열 때만 받는다(`dynamic` + 마운트 후 fetch).

### 단순화가 나라를 망가뜨렸는가

08 §3: "단순 polygon 단순화 때문에 작은 나라가 사라지면 출제하지 말고 원인 해결".
그래서 빌드 스크립트가 만들고 나서 **스스로 검증한다** — 나라마다 대표점이 여전히
자기 나라 안에 들어가는지, 원본 대비 면적이 12% 넘게 어긋나지 않는지. 어긋나면 그
나라를 `quizEligible:false` 로 내리고 화면에 이름을 찍는다. 조용히 넘기지 않는다.

**처음 돌렸을 때 피지가 통째로 사라지고 통가가 14.7% 어긋났다.** 원인은 날짜변경선
이었다. 허용오차를 나라 전체 bbox 로 재고 있었는데, 피지는 경도가 -180 과 +180
양쪽에 걸쳐 있어 폭이 360도로 잡혔고 → 허용오차가 폭발 → 섬이 전부 한 점으로
뭉개졌다. 조각 하나씩 재도록 고치니 둘 다 살아났다(피지 42개 폴리곤 보존).
08 §3 이 "날짜변경선" 을 따로 짚어 둔 이유를 직접 밟은 셈이다.

결과: **66개국 전부 보존, 출제 가능 63개**, 대륙 6개 모두 포함(아프리카 12 · 아시아 15 ·
유럽 13 · 북아메리카 10 · 남아메리카 10 · 오세아니아 6). 출제에서 빠진 3개는
`major-countries.json` 이 원래 `quizEligible:false` 로 둔 분쟁 지역이다(SAH·PSX·TWN).
08 §3 "분쟁 지역은 … 검증된 출제 대상으로 확정되기 전 정답 범위에서 제외" 그대로다.

## 클릭 좌표를 SVG 에 맡긴 이유

viewBox 를 `0 0 360 180` 으로 두면 SVG 안의 좌표가 **곧 경위도**다(x = 경도+180,
y = 90-위도). 클릭 지점은 `getScreenCTM().inverse()` 로 되돌린다. 정거원통 투영이라
역변환이 근사가 아니라 **정확**하다 — 왕복 오차를 재 보니 최대 1.4e-14도다.

덕분에 08 §4 의 "글자 크기 변경으로 지도 크기/클릭 좌표가 깨지지 않아야 한다" 가
저절로 지켜진다. 글자 크기는 SVG 바깥 이야기라 좌표계를 건드릴 수 없다. 실제로
루트 글자 크기를 24px 로 올려 놓고 브라질 대표점을 눌러 봤고 그대로 맞았다.

## 계약별로 한 것

- **해상 클릭**은 오답이 아니다. "바다예요. 나라 안을 골라 주세요." 를 띄우고 **이미
  고른 나라를 지우지 않는다**(08 §3). 가장 가까운 나라를 찾아 정답 처리하는 코드는
  아예 없다 — 안에 들어갔을 때만 그 나라다.
- **경계가 겹치면** 임의로 하나 고르지 않고 후보를 말해 주고 다시 고르게 한다.
- **정답이 새지 않는다.** 시험 중에는 지도에 국가명 라벨이 하나도 없고, 정답 강조도
  없다. 확정한 뒤에만 이름이 나온다(08 §2 "확정 전에는 국가명 tooltip 을 숨긴다").
- **힌트는 대륙 → 지역 강조 → 정답 위치** 세 단계(08). 힌트를 쓴 문제는 결과에서
  독립 해결과 **따로** 센다 — 한 점수로 합치지 않는다.
- **중복 출제 없음.** Fisher-Yates 로 섞고 앞에서 자른다. 범위가 요청 수보다 적으면
  가능한 수로 줄이고 그 사실을 화면에 적는다(`shortOf`).
- **resize 가 문제를 다시 뽑지 않는다.** 시드를 state 로 한 번만 정한다. 1366 →
  820 → 390 으로 흔들어도 문제가 그대로인 것을 확인했다.
- **끌기와 고르기를 가른다.** 8px 넘게 움직이면 이동이지 선택이 아니다.
- **작은 나라 선택 보조**는 확대다(최대 12배). 확대해도 국경선 굵기는 화면에서 같게
  보이도록 배율로 나눈다.
- **매 프레임 다시 그리지 않는다**(08 §4). 나라 path 문자열은 번들이 올 때 한 번만
  만들고, 확대·이동·정답 공개는 viewBox 숫자와 색만 바꾼다.
- **반응형**: 태블릿 세로 820×1180 은 큰 지도 → 문제 세로 쌓기, 가로 1024×768 은
  지도 + 문제 패널 나누기. 셋 다 가로 넘침 0, 조작 버튼 6개 전부 44px 이상.

## 검사

```
node scripts/test-world-map-quiz.mjs     # 순수 로직 24항목
node scripts/verify-world-map-quiz.mjs   # 브라우저 화면 24항목
```

둘 다 전부 통과. 로직 쪽은 화면 없이 `lib/worldMapQuiz.ts` 를 그대로 돌린다.
화면 쪽은 실제로 대표점을 눌러 보고(KOR·BRA·AUS·EGY·JAM), 태평양을 눌러 보고,
끌어 보고, 화면을 흔들고, 한 판을 끝까지 돌린다. Firebase·/api 호출 0건도 확인했다.

## 아직 안 한 것 (정직하게)

- **지도에 66개국만 그려져 있다.** 나머지 육지는 바다처럼 비어 있다. 아이가
  "여기는 아무 나라도 없구나" 로 읽지 않도록 지도 아래에 "이 지도에는 우리가 배우는
  나라만 그려져 있어요" 를 적어 두었지만, 제대로 하려면 Natural Earth 의 land polygon
  을 따로 받아 배경 육지를 깔아야 한다. 그 데이터의 출처·라이선스를 공식 출처에서
  확인하는 일이 먼저다(08 §3) — **아직 하지 않았다.**
- **나라 이름 16개는 한국어·영어만 있다.** `lib/gameData.ts` 의 COUNTRIES(15개 언어)에
  없는 나라들이라 `SUPPLEMENT_NAMES` 로 보충했는데, 나머지 13개 언어를 기계번역으로
  채우면 검수받지 않은 나라 이름이 아이에게 그대로 나간다. 없는 언어는 영어로 떨어지게
  두었다. **선생님 검수 대상**이다: MG KP JM GT PA CR HT DO EC BO PY UY FJ PG WS TO.
- **자유 탐색·검색·시간 도전은 없다.** 08 이 "검색은 문제 범위를 고르는 설정/자유
  탐색에만" 이라고 정해 두었는데, 이번에는 문제 풀이 모드만 만들었으므로 검색 자체가
  없다. 규칙을 어긴 것은 아니지만 기능이 빠진 것은 맞다.
- **대륙 범위 고르기는 로직에만 있고 화면에는 없다.** `planQuiz(..., {continents})` 가
  동작하고 검사도 하지만 아이가 고를 UI 를 아직 붙이지 않았다.
- **지구본 3D 실패 시 이 평면지도로 이어받는 경로는 없다.** 08 §4 가 요구하는
  fallback 인데, 두 화면이 아직 상태를 주고받지 않는다.
- **하네스 10칸 감사는 아직이다.** 화면은 생겼고 재현 경로도 있지만 기기 5종 × 글자
  2종 캡처와 감사 기록은 비어 있다. 새로 만든 화면이라 'before' 는 없다 — 그 사실을
  적고 after 만 남기는 것이 맞다.
