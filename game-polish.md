# game-polish — 대표 게임 고도화 기록 (U10)

06 문서 §8 이 요구하는 기록물이다. 게임별로 **무엇이 어땠고 / 무엇을 바꿨고 /
어떤 기술을 썼고 / 무엇을 2D 로 남겼고 / 성능과 대체 경로는 어떤지**를 적는다.

측정은 전부 **Chrome viewport 시뮬레이션**이다. 실기기(태블릿·크롬북)는 없다.
06 §7 이 요구한 대로 실기기 PASS 를 주장하지 않는다.

---

## 꿀벌 월드 마블 (대표 구현, G02)

### 쓰는 기술과 쓰지 않은 기술

**WebGL 을 쓰지 않았다.** 06 §3 은 "먼저 2.5D 프리렌더 에셋 + DOM 레이어로
대표 한 턴을 만들어 품질·가독성을 검수하고, 실제 회전·조명·말 이동에 이점이
있는 부분만 Three.js" 라고 정한다. 마블 판은 고정 시점의 정사각 링이라 회전도
자유 카메라도 필요 없고, 30칸의 작은 글씨가 원근에 들어가면 06 §2 가 금지한
"글씨가 기울어 읽기 어려워지는" 상태가 된다. 그래서 깊이는 CSS 그림자·높이
차로만 만들었다.

그 결과 **3D 모드의 추가 전송량은 0 바이트다.** 06 §7 의 3MB 예산을 쓰지
않았고, 새 의존성도 없다. WebGL 초기화 실패·context lost 로 빈 화면이 될 경로
자체가 없다.

(지구본은 이미 three.js 를 쓴다. 꿀벌 마을도 `components/VillageMap3D.tsx` 로
three.js 를 쓴다 — 둘 다 회전·시점 이동이 게임의 본질이라 3D 가 맞는 자리다.)

### before → after

| 부분 | before | after |
|---|---|---|
| 판 | 평평한 사각형. `border 3px` + 옅은 그림자 하나 | 아래쪽에 두께(옆면 2단)와 책상 접촉 그림자 2단. 판 밖 여백을 줘 그림자가 잘리지 않는다 |
| 칸 | 전부 같은 높이의 납작한 사각형 | 낮은 블록. 기본 1px · 내가 가진 칸 2px · 도착 칸 3px + 위로 2px 이동 + 빛 |
| 도착 강조 | 테두리 색 + 옅은 glow | 위로 솟음 + 굵은 테두리 + 그림자 (색만으로 알리지 않음) |
| 주사위 굴림 | **없음.** 누르면 그 자리에서 결과가 정해지고 바로 이동 | 누름 → 0.7초 굴림 → 확정 → 이동 |
| 굴리는 동안 | `rolling = phase === "moving"` 이라 **말이 움직이는 내내 주사위가 돌고**, 확정 숫자는 이동이 끝나야 보였다 | 굴림 구간에만 돌고, 이동 중에는 확정 숫자가 그대로 있다 |
| 이동 속도 | 칸당 200ms 고정 → 12칸이면 2.4초 (예산 초과) | 남은 칸으로 나눠 총 1.5초 예산, 칸당 90~200ms 로 제한 |
| 움직임 줄이기 | 지원 없음 | 굴림·칸별 이동을 건너뛰고 도착 상태로 바로 간다 |
| 도착 요약 | "칸 #6" 과 턴 종료 버튼뿐. 판 가운데가 통째로 빔 | 누가 · 몇을 굴려(🎲 a + b = n) · 어디에 왔는지(랜드마크 이름)를 한 카드에 |
| 말 | 타일 컴포넌트 **안**의 작은 배지. 칸이 바뀌면 사라졌다 나타남. 같은 칸에서 구석에 겹쳐 쌓임 | 보드 위 **별도 레이어**. 칸 사이를 미끄러지고, 같은 칸이면 2×2 로 비켜 앉음. 발밑 접촉 그림자, 이동 중인 말만 통통 튐 |

### 규칙과 연출의 분리 (06 §3)

굴림 값은 **버튼을 누르는 순간** 정해져 ref 에 담기고, 연출이 끝날 때 그 값을
그대로 reducer 에 넘긴다. 굴러가는 그림이 결과를 다시 뽑지 않는다
("시각 물리가 재추첨하지 않음"). 연출 타이머는 턴 상태를 따로 들고 있지 않고,
`phase` 를 읽어 **언제 보여줄지**만 정한다. 그래서 재렌더·창 이동 뒤에도
현재 state 그대로 복원된다.

### 측정 (1366×900, Chrome, dev 서버)

| 항목 | 값 | 06 목표 |
|---|---|---|
| 누름 → 숫자 확정 | **798~818ms** | 0.6~1초 ✓ |
| 굴리는 동안 눈에 보인 조합 | **6가지** | "굴렸다" 가 느껴질 것 ✓ |
| 움직임 줄이기 ON — 확정까지 | **363ms** (연출 생략, 값은 즉시) | 결과를 바로 ✓ |
| 움직임 줄이기 ON — 굴림 조합 | **2가지** (사실상 안 돎) | 흔들림 생략 ✓ |
| 칸별 이동 상한 | 1500ms ÷ 남은 칸, 칸당 최소 90ms → **20칸 이동도 1.8초** | 총 2초 이내 ✓ |
| 3D 추가 전송량 | **0B** (WebGL 미사용) | 3MB 이내 ✓ |
| 말이 한 턴에 지나간 좌표 | **29개** (220,268 → 854,463) | 칸마다 이동이 보일 것 ✓ |
| 같은 칸 두 말 사이 거리 | **41px** (말 지름 34px) | 서로 가리지 않을 것 ✓ |

한 턴 전체 흐름(준비 → 굴림 → 이동 → 도착 → 행동)이 실제로 이어지는 것을
Playwright 로 확인했다: 시작 → 굴리기 → 숫자 확정 → 말 이동 → 황금열쇠 카드,
그리고 다른 판에서는 도시 도착 → 구매 카드(살래요/안 살래요).

### 아직 안 한 것 (정직하게)

- 말 레이어는 **했다**(`PieceLayer.tsx`). 다만 이동은 칸 중심에서 칸 중심으로
  잇는 직선 보간이라, 모서리를 돌 때 판 안쪽을 가로질러 지나간다. 실제 경로를
  따라 도는 곡선 이동은 아직이다.
- fps 는 **쟀다**(아래 성능 절 참고). 입력 지연은 재긴 했으나 측정 방법에
  한계가 있어 그대로 목표 미달로 단정하지 않는다 — 그 절에 적었다.
- 판을 기울이지 않기로 한 결정은 §2 의 "고정된 완만한 시점" 을 **기울기 없이**
  해석한 것이다. 기울인 시안을 만들어 가독성과 비교해 본 것은 아니다.
- G03·G05 는 **끝냈다**. 아래 "G03" 절과 "윷놀이 · 할리갈리 · 문화 퍼즐" 절 참고.

### G03 — 퀴즈·찬스·겹친 말·모서리·결과 (2026-09-14)

문서가 "실제 존재하는 상태를 inventory 에서 확인" 이라 못 박았으므로, 손으로
훑지 않고 **시드를 고정한 fixture 를 자동으로 굴려** 무엇이 진짜 나오는지 셌다:
`node scripts/marble-inventory.mjs`. 내부 state 를 훔쳐보지 않고 DOM 으로만
판별한다(그래서 `PieceLayer` 의 말에 `data-mb-piece/tile/stack` 을 달았다 —
화면에는 영향이 없다).

inventory 결과: **퀴즈·찬스·겹친 말·결과 네 상태 모두 실제로 나온다.**
결과(gameover)는 80턴으로는 못 갔고 시드 7·400턴에서 나왔다(15라운드 종료).
모서리는 이미 구조가 잡혀 있었다 — 모서리 91×91, 변 69×91 로 측정됐다.

고친 것:

- **겹친 말이 칸 위로 밀려 있었다.** 자리표가 2×2 라 둘일 때는 언제나 첫 줄만
  차서 dy 가 둘 다 -2.9% — 두 말이 나란히 위로 올라가 칸 테두리를 넘었다.
  마블은 2인 대전이 가장 흔하니 이게 평소 모습이었다. 둘일 때는 좌우로만
  비키게 해 세로 중심을 칸 한가운데로 되돌렸다(말 y 140 → 161, 칸 중심 근처).
- **퀴즈·찬스 뒤가 흰색 55% 막이었다.** 보드도 카드도 크림색이라 밝기가 비슷해
  카드가 위에 있는 것으로 읽히지 않고 보드만 바래 보였다. 어두운 막
  (`rgba(41,37,31,.45)`)으로 바꾸니 카드가 그대로 떠오른다.
- **대화상자가 겹쳐 선언돼 있었다.** 바깥 막과 안쪽 카드가 둘 다
  `role="dialog" aria-modal="true"` 였다. 막에서 뺐다 — 막이 하는 일은 가리는
  것뿐이고, 대화상자는 카드다.
- **로그가 새까만 상자였다.** 옆에 나란히 선 '더보기'·'처음부터' 는 크림 표면에
  갈색 테두리인데 이것만 `rgba(41,37,31,.92)` 였다. 같은 표면 토큰으로 맞췄다.
- **그 로그가 62px 로 눌려 한 줄짜리 글이 네 줄로 쪼개지고 잘렸다.**
  `flex: 1 1 180px; min-width: 140px` + 줄바꿈 허용으로 147px 를 확보했다.
- **결과 화면이 거의 빈 흰 상자였다.** 판 안쪽을 꽉 채우는 카드 안에 이모지
  트로피 하나, 이름 한 줄, 글자 두 줄짜리 순위가 떠 있어 위아래가 텅 비었다.
  15라운드를 끝낸 아이가 받는 화면이 그것뿐이었다. 이긴 꿀벌을 **자기 얼굴로**
  크게 보여 주고(말에 쓰는 그 스킨), 순위는 메달·얼굴·꿀 막대로 바꿨다 —
  숫자만 늘어놓으면 누가 얼마나 앞섰는지 알려면 뺄셈을 해야 하는데 막대는
  길이가 곧 답이다. 순위표를 **파산 종료에서도** 보여 준다(예전에는 라운드 캡
  종료에서만 나와서 파산으로 끝나면 화면이 더 비었다).

남은 것: 결과 카드는 여전히 판 안쪽을 꽉 채우므로 내용 위아래에 빈 자리가
있다. 카드 자체를 내용 크기로 줄이면 다른 phase(구매·통행료·축제)의 배치가
같이 바뀌어서 이번에는 손대지 않았다.

### 성능 (06 §7)

`scripts/perf-games.mjs` 로 쟀다. **실기기가 아니다** — headless Chrome, dev
서버(프로덕션보다 느리다), 1366x768. 06 §7 이 허용한 CPU throttle 대리 측정이며
실기기 PASS 를 주장하지 않는다. dev 서버는 소스맵·HMR 을 달고 돌므로 이 값은
**하한(이보다 나쁘지 않다)** 으로 읽어야 한다.

| 게임 | CPU | fps (목표 ≥30) | p50 | p95 (목표 ≤33ms) | 긴 프레임 >50ms | 최악 |
|---|---|---|---|---|---|---|
| 마블 | 1x | **60** ✓ | 16.7ms | **16.8ms** ✓ | 0 | 18.2ms |
| 마블 | 4x | **60** ✓ | 16.7ms | **16.9ms** ✓ | 0 | 19.7ms |
| 지구본 | 1x | **60** ✓ | 16.7ms | **16.9ms** ✓ | 0 | 18.1ms |
| 지구본 | 4x | **57** ✓ | 16.7ms | **18.5ms** ✓ | 1 | 66.7ms |
| 할리갈리 | 1x | **60** ✓ | 16.7ms | **16.8ms** ✓ | 0 | 20.1ms |
| 할리갈리 | 4x | **60** ✓ | 16.7ms | **16.8ms** ✓ | 0 | 17.6ms |

프레임 예산에는 여유가 있다. CPU 를 4배 느리게 걸어도 마블·할리갈리는 60fps 를
유지하고, three.js 를 쓰는 지구본만 57fps 로 내려가며 66.7ms 짜리 긴 프레임이
한 번 나온다. 다중 canvas 나 RAF 누수는 없었다(마블·할리갈리 canvas 0,
지구본 1).

**입력 지연은 목표(100ms)를 넘는 값이 나왔다 — 다만 측정을 믿을 수 없다.**
마블 109ms / 지구본 509ms (CPU 4x 에서 330ms / 1723ms). 내 측정기는 클릭 뒤
`MutationObserver` 가 잡은 첫 변화의 다음 프레임까지를 쟀는데, 이건 06 §7 이
말하는 "클릭 시 **시각적 피드백**" 이 아니라 **그 동작이 만들어 내는 첫 DOM
변화**다. 화면마다 첫 조작이 하는 일이 달라(지구본은 모드 전환) 서로 비교할
수도 없다. 눌림 상태(transform/배경 변화)가 나타나는 시점을 재도록 고쳐야
하고, 그 전까지는 **미달로 단정하지 않는다.**

---

## 지구본 (U11 — 배치만)

three.js 기반은 이미 있었다. 이번에 한 것은 배치다: 셸 자신의 폭 기준
컨테이너 질의 + 가로 방향에서 2열(왼쪽 지구본 / 오른쪽 정보 패널), 패널 폭
`clamp(260px, 28cqi, 360px)`, 2열에서는 옆 칸을 미리 비워 둬 나라를 눌러도
지구본이 줄지 않는다(폭·높이 변화 0.0%).

측정: 1366×768 / 1280×800 / 1024×768 에서 2열, 캔버스가 열의 100%, 캔버스
중심이 캔버스 자신에 히트(패널이 덮지 않음), 가로 넘침 0. 768 세로·390 은
계약대로 1열.

06 §6 이 요구한 나머지(나라 선택 하이라이트 개선, 시점 원위치, 드래그와 클릭
구분)는 아직이다.

## 꿀벌 마을 (3D)

`components/VillageMap3D.tsx` (834줄, three.js, `dynamic` + `ssr:false` 로
게임 진입 시에만 로드)가 이미 동작한다. fixture 로 확인: 캔버스 540×440,
WebGL 정상, 콘솔 오류 0.

06 §7 이 요구하는 성능 예산·DPR 상한·2D 대체 경로는 아직 확인하지 않았다.

## 윷놀이 · 할리갈리 · 문화 퍼즐

G05 완료(2026-09-14). 06 §6 이 세 게임에 요구한 것은 "깊이"이지 3D 가 아니다.
셋 다 **WebGL 도 canvas 도 쓰지 않았다** — 그림자·받침·:active 만으로 냈다.
그래서 §7 의 성능·입력 예산을 위협하지 않는다(아래 측정).

검증: `node scripts/verify-g05.mjs` — 11개 항목 전부 통과. 눈이 아니라
computed style 과 SVG 노드를 직접 읽는다.

### 문화 퍼즐 — 조각의 높이·집기

- 조각에 두께 그림자 두 겹(`0 2px 0` + `0 3px 5px -1px`). 판 위에 **얹힌** 것으로 읽힌다.
- 고른 조각은 `translateY(-4px) scale(1.03)` 로 **집어 든다**. 측정값 -4.0px.
- 다 맞추면 틈(`gap:0`)과 함께 높이·그림자도 없앤다 — 마지막엔 한 장의 그림이어야 한다.
- 드래그는 원래 없었다(클릭 → 클릭 교환). 06 §6 의 "드래그 외 선택 후 놓기"는 이미 충족.

덤으로 고친 것(깊이 작업 중 발견): **판이 화면 밖으로 잘리고 있었다.**
폭만 보고 키운 탓에 1366×768 에서 판이 651px 가 돼 아래 **120px**, 1024×768 에서
**69px** 이 접혔다. 조각을 바꾸려면 그림 전체를 한눈에 봐야 하는데 아랫줄이
안 보였다. `max-width: min(100%, 60svh)` 로 묶어 둘 다 **0px** 이 됐다(판 461px,
조각 하나 153px). 390×844 는 아직 34px 남는다 — 여기서는 판(358px)이 폭에 묶여
있어 더 줄이면 조각이 작아진다. 판 위쪽 안내를 줄이는 쪽이 맞고, 아직 안 했다.

### 할리갈리 — 카드 두께·종 눌림·짧은 넘김

- 카드 아래로 겹친 그림자 3겹 → '쌓인 카드 더미'. 테두리는 과일 색이라 손대지 않았다.
- 맞은편 자리는 카드를 180° 돌려 놓는데 **box-shadow 도 같이 돈다**. 두께가 위로
  올라가 빛이 아래에서 오는 꼴이 돼서, 그 자리만 그림자 방향을 미리 뒤집었다.
- 빈 자리(`.hg-cardempty`)는 두께도 넘김도 뺐다 — 없는 카드가 있는 것처럼 보였다.
- 넘김 180ms. 종을 치려고 **보는** 화면이라 이보다 길면 판단을 가린다. 파일 높이를
  key 로 써서 같은 과일·같은 개수가 연달아 나와도 반드시 다시 재생된다.
- 종은 4px 턱 위에 있다가 `:active` 에 그만큼 내려앉는다(측정 +4.0px). **CSS 만**
  쓰므로 자바스크립트가 끼어들지 않는다 — 누른 프레임에 바로 반응한다.

성능(`scripts/perf-games.mjs`, headless Chrome·dev 서버): 할리갈리 **60fps ·
p95 17ms**, 긴 프레임 0, canvas 0. 깊이를 넣기 전과 같다.

### 꿀벌 윷놀이 — 나무 윷·입체 말·접촉 그림자

- 윷가락은 통나무를 쪼갠 것이라 **가로**로 둥글어야 한다. 세로 그라데이션만 있어
  납작한 알약으로 보이던 것을 가로 결(둥근 단면) + 세로 결로 바꿨다.
- 엎어진 면은 톱으로 자른 평평한 단면이라 둥근 결을 빼고 가장자리 테만 남겼다.
- 윷가락 밑에 바닥 그림자 하나. 던지는 동안 옅어지고 퍼진다(접촉 그림자).
- 판 위의 말은 **팀 색 후광**(drop-shadow 팀색)을 쓰고 있었다. 후광은 사방으로
  퍼져 말이 판에 놓인 게 아니라 떠 보였다. 눌린 타원 그림자 + 팀 색 받침으로 바꿨다.
  팀 구분은 받침이 맡는다. 남은 팀 색 후광 0개.
- 대기 말에는 받침을 두지 않았다. 꿀벌 몸이 거의 다 덮어 보이지도 않으면서
  좁은 대기칸에 색 타원 넉 장이 더해져 지저분해졌다(캡처로 확인하고 되돌렸다).

### 하지 않은 것

- 윷 던짐·말 이동의 길이는 손대지 않았다. 06 §6 은 "짧은 던짐/이동"을 말하는데
  지금 값이 긴지 아닌지 재지 않았다.
- 할리갈리 입력 지연은 **숫자로 재지 못했다**. `perf-games.mjs` 가 준비 화면을
  통과하지 못해 측정칸이 비어 있다(`입력 - ?`). 종이 CSS `:active` 라 지연이
  생길 구조가 아니라는 것은 말할 수 있지만, 잰 값은 아니다.
- 390px 퍼즐판 34px 잘림(위 참고).
