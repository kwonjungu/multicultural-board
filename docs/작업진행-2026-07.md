# 작업 진행 로그 — 수정설계서-2026-07 구현

> **2026-07-02 대규모 통합 완료 (커밋 d614e13, push 됨).** 원격(다른 세션의 65커밋: 의견나누기 멀티모달·그림책 DrawBoard/자유읽기·화이트보드·디자인 개편 등)을 베이스로 삼고, 이 세션의 설계서 12항목을 그 위에 재이식했다. 재이식 전 로컬 원본은 `backup/claude-desktop-20260702` 브랜치에 보존.
> 규칙: ① `npm run build` 통과 후 커밋 ② 커밋은 `GIT_AUTHOR_NAME/EMAIL` env 방식(git config 수정 금지).

## 상태표 (전 항목 통합 후)

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| 8 | 발음 업로드 실패 (서버 업로드 통일+재시도+통과인정) | ✅ | upload route/vocabRecordings 는 백업 체크아웃, VocabRecorder 는 원격 위 재적용 |
| 6 | 월드마블 주사위 2D 폴백 | ✅ | 원격의 "크게(58/84)" 사이징 유지하며 2D 전환 |
| 2 | 컬럼 관리 | ✅ (원격 자체구현 채택) | 원격 d120614+091ae1d — 소통판 내 추가/삭제 이미 구현됨 |
| 5 | 복습 원문+번역 2줄 고정 | ✅ | showOriginal 토글 제거 재적용 |
| 9 | 질문 추가+노출 페이지 | ✅ | QuestionEditor tier/pageIdx + ＋질문 추가 |
| 10 | 주아체 무지개 배너 | ✅ | 메인/방입장/허브 — 원격 새 일러스트 배경과 공존 |
| 1 | 복습 응답 대댓글 | ✅ | submitResponse set→update(원격 bookAnswers fire-and-forget 은 유지) |
| 3 | 복습 중 챗봇 토글 | ✅ | 기본 OFF, TeacherPageControls 스위치 |
| 4 | 다중 챗봇+뒤로가기 | ✅ | 원격 per-client 선택 위에 챗로그 캐릭터별 분리(`chat/{clientId}/{characterId}`) + onBack |
| 7 | 전시장 전원+좋아요/댓글 | ✅ | lib/gallery.ts(runTransaction 하루1회). **시각 미표시**(원격 785f1f4 정책 준수) |
| 11 | 튜토리얼 섹션 직접 진입 | ✅ **4개 섹션** | navigate 스텝 — board+games+dashboard+vocab 진입 (학생/교사) |
| 12 | 게임 콘텐츠 다국어 | ✅ 광역 확산 | 아래 상세 |

## #12 확산 완료 내역

- 코어: `lib/gameI18n.tsx` (useGameText/GameText/translateGameText/prefetchGameTexts, localStorage 캐시 `gi18n:*`).
- 적용(JSX 렌더 → GameText): **BeeWorldMarble**(Tile/ChanceCard/ActionPanel + index 프리페치), **EmotionQuiz**, **WordTower**, **GreetingRelay**(보기), **HoneyTaboo**, **StoryCubes**, **WouldYouRather**, **CountryGuess**, **TwentyQuestions**, **BeeCafe**(Kitchen/MenuDeck/ServeResult/StepSequencer/OrderScene).
- 의도적 유지(tr 그대로): aria-label·TTS 인자·문자열 조립(OrderScene 주문 문구, QuizCard prompt 템플릿), **DrawGuess 정답 문자열(채점 로직 — 번역 치환 시 채점 왜곡)**, WordMemory/SpotIt(useMemo 내 문자열 카드 — hook 불가), WYR ResultBar label prop.
- 남은 선택 과제: QuizCard(marble) promptSecondary 영어 하드코딩, WordMemory/SpotIt 구조 개편 필요 시.

## #11 앵커 맵

hub-header · hub-section-board/-games/-dashboard/-vocab (HomeHub) / board-header·board-fab·board-emotion-fab (PadletBoard) / games-header (GameRoom) / praise-header (PraiseHive) / vocab-header (VocabHub). RoomPage 가 `TutorialBus.on("tutorial-navigate")` 로 hubView 전환.

## 통합 시 내린 결정 (검토 기록)

- **#2/#4 선택**: 원격 구현이 이미 실전 반영돼 있어 원격 채택. #4는 원격에 없던 "챗로그 분리+뒤로가기"만 추가.
- **timeAgo**: 원격이 칭찬 시점 비노출 정책으로 함수 자체를 제거 → 전시장 댓글도 시각 미표시로 통일.
- **submitResponse**: 원격의 bookAnswers(자유읽기 영속 사본)·liveBoard 갱신 로직 유지하면서 메인 쓰기만 update 로 변경. bookAnswers 사본에는 댓글 미포함(자유읽기에서 댓글 안 보임 — 허용).
- 원격 신규 의존성 `pptxgenjs` 로 `npm install` 필요했음 (다른 기기에서 pull 후 필수).

## 2026-07-02 2차 감사 — "복습 = 자유 읽기" 누락 케이스 전수 점검

사용자 지적("토글/컬럼 관리 미구현")의 근본 원인: 원격의 자체 변형 구현을 요구사항 충족으로 오판 + **진짜 복습 무대(자유 읽기)에 기능이 안 붙음**. 전수 재점검 결과:

| 항목 | 점검 결과 | 조치 |
|---|---|---|
| #2 컬럼 | 원격은 관리모달에 컬럼관리 잔존 + '칸 삭제 모드' 방식(더블클릭 없음) | ✅ 요구대로 재구성: 관리모달 완전 제거, 헤더 ⚙ 팝오버, **더블클릭 삭제**(confirm+undo). 칸삭제모드/롱프레스 제거 |
| #3 챗봇 토글 | 세션 중 토글만 있고 자유 읽기(복습)에 없음 | ✅ `Storybook.chatEnabled` 책별 플래그 + 교사 목록 `🐝 챗봇` 칩 + FreeReader 물어보기 버튼/오버레이 |
| #1 댓글 | 자유 읽기(FriendAnswers)에 없음 + **responses 하위 저장이라 endSession 때 댓글 전멸** | ✅ 저장 경로를 영속 `bookAnswers/{book}/{q}/{resp}/comments` 로 이전(세션 fruit 모달·자유읽기 공유). submitResponse 의 bookAnswers set→update(재제출 시 댓글 보존). FriendAnswers 에 답변별 댓글 표시+입력(안전검사) |
| #5 2줄 | FriendAnswers 가 `trans ‖ text` 로 원문 숨김 | ✅ 번역 메인 + 📜 원문 항상 표시(답변·댓글 모두, 배치 번역에 댓글 포함) |
| #8 | 원격 신규 '말하기 문항'(VocabTest)은 STT만 사용, 저장 업로드 없음 | 이상 없음 |
| #7 | race 탭 교사/학생 모두 접근 가능(교사 기본 탭) | 이상 없음 |
| #10 | 원격 태그라인과 중복 없음 | 이상 없음 |
| #12 | BeeTreasureHunt 장면 이름(콘텐츠) tr 잔존 | ✅ GameText 변환. `lab()` 헬퍼(트레저헌트/틀린그림)는 UI 라벨이라 정책상 유지 |

## 미해결 / 실기기 검증 TODO

- (#8) 배포 후 태블릿에서 녹음 저장, (#6) 주사위 렌더 확인.
- Firebase 규칙: 신규 노드(`storybook/responses/**/comments`, `rooms/*/gallery/**`) 클라이언트 쓰기 허용 점검.
- 튜토리얼 navigate 실기기 확인(첫 입장 학생/교사 각 1회).
